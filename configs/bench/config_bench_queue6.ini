; Benchmark: process 6 songs (queue inflation for determinism).
; Inherits shared bench settings from ../common/bench_common.ini.
[IterationEngine]
_extends = ../common/bench_common.ini
; Override: small queue for fast iteration.
SongQueueLimit = 6
IgnoreResumeQueue = true