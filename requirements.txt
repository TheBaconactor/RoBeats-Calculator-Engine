# Runtime dependencies

# Pinned to known-good versions (Python >= 3.10). Update intentionally with a test run.
numpy==1.26.4
numba==0.59.1
taichi==1.7.4
psutil==7.1.3
requests==2.32.3
python-dotenv==1.2.1
cachetools==6.2.4
