# Python bytecode
__pycache__/
*.py[cod]

# Test/coverage caches
.pytest_cache/
.coverage
htmlcov/

# Type checker / linter caches
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Taichi cache
.taichi/

# Local database journaling files
*.db-journal
*.db-shm
*.db-wal

# OS/editor noise
Thumbs.db
Desktop.ini
.DS_Store

# Profiling artifacts
*.prof
*.pstats

# Sensitive credentials (NEVER commit these!)
Discord.env
*.env
.env

# Paths cache (local config)
paths_cache.json

# IDE/AI tool config (local)
.claude/
.cursor/

# Local databases (generated during runtime)
evolution*.db

# Website export folder
RoBeatMetaWebsite/

# RoBeats Rojo project (legacy game code)
RoBeats-Rojo/

# Local bin folder (logs, temp files, local databases)
bin/
gear_optimizer/bin/

# Generated results files
general_meta_results.json

# Local benchmark / run artifacts
artifacts/
tmp_dbg/
analysis.txt
tmp_general_meta_debug*.db*
runs*/
run_*.log

# Local scratch/profiling DBs, tool state, and ad-hoc validation output (never checked in)
.calc/
.reasonix/
compare_results.json

# Agent/benchmark scratch: .ai-bridge holds a throwaway benchmark virtualenv (frontier-bench-venv,
# ~262MB of taichi/numpy binaries); docs/research + tools/research are ad-hoc benchmark artifacts.
.ai-bridge/
docs/research/
tools/research/
